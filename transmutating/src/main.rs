#![allow(unused)]
use std::sync:
fn main() {
    let number = Arc::new(5);
    let another_number = Arc::new(6);
    pub fn main() {
        cout << "this is teh main function out there "<<endl;
    }

}
